---
description: Attach a project path to the current taskwatch task
---
Attach a project path to the current taskwatch directory. Ask the user for the absolute path to their project, then use `taskwatch directory attach <name> <path>` to register it.